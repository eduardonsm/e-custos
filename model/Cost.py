class Cost:
    def __init__(self, nome, valor, categoria, is_direto, is_fixo, is_relevante, is_eliminavel, is_oculto):
        self.nome = nome
        self.valor = valor
        self.categoria = categoria
        self.is_direto = is_direto
        self.is_fixo = is_fixo
        self.is_relevante = is_relevante
        self.is_eliminavel = is_eliminavel
        self.is_oculto = is_oculto
    